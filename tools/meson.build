configure_file(input: 'imgconv_lib.py',
  output: 'imgconv_lib.py',
  copy: true,
  install_dir: meson.project_build_root(),
  install: true,
)

img_converter = configure_file(input: 'imgconv.py',
  output: 'imgconv.py',
  copy: true,
  install_dir: meson.project_build_root(),
  install: true,
)
